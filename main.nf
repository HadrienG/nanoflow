#!/usr/bin/env nextflow

params.reads = ''
params.assembler = ''
params.consensus = ''

reads = file(params.reads)

process adapter_trimming {
    input:

    output:

    """
    """
}

process assembly {
    input:

    output:

    """
    """
}

process consensus {
    input:

    output:

    """
    """
}
